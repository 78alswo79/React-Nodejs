``` Nodw.js를 먼저 설치합니다. 설치 후 터미널(CMD)에서 버전을 확인합니다.```
```
Bash
node -v
npm -v
```

```
Bash
cd React-nodejs
npm init -y >>> node.js 설정파일 생성
```

```
Bash
npm install express >>> 서버 구축을 위한 Express 설치
node app.js >>> 서버실행
localhost:3000
```
